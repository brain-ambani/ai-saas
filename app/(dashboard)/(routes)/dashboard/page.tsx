import { Button } from "@/components/ui/button";

export default function Dashboard() {
  return (
    <main>
      <p className="text-3xl text-green-500">Dashboard page(protected)</p>
    </main>
  );
}
